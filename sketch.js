function setup() {
  createCanvas(windowWidth, windowHeight);
  // background(0);
}

function draw() {
  background(0);

  textAlign(CENTER);
  textSize(50);
  fill(0,255,0);
  text("Jiwon", mouseX, mouseY);
}